type HttpResourceCache @table(database: "cache") @export {
	cacheKey: String! @primaryKey
	statusCode: Int!
	headers: Any
	content: Blob
	lastCached: Date
	staleWhileRevalidateUntil: Date
	staleIfErrorUntil: Date
}
